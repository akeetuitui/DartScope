services:
  - type: worker
    name: dartscope-telegram-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_bot.py
    envVars:
      - key: DART_API_KEY
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
